function traverseArr(array) {
  for (let i = 0; i < array.length; i++) {
    console.log(array[i]);
  }
}

// პირველი გამოძახება
traverseArr([10, 20, 30, 40]);

// მეორე გამოძახება
traverseArr(["ვაშლი", true, 3.14, { name: "ანა" }, [1, 2]]);